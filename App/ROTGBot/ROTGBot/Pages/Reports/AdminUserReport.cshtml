@page
@model ROTGBot.Pages.Reports.AdminUserReportModel
@{
    ViewData["Title"] = "Отчёт по пользователям по отправленным сообщениям";
}

<h1>Отчет</h1>

<p>
    <!--Фильтры-->
</p>

@foreach (var item in Model.Report.Items)
{
    <div>@Html.DisplayNameFor(modelItem => item.User) : @Html.DisplayFor(modelItem => item.User)</div>
}

<div>@Html.DisplayNameFor(modelItem => Model.Report.Total) : @Html.DisplayFor(modelItem => Model.Report.Total)</div>

<table class="table" id="reportTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Report.Items[0].User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Report.Items[0].)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Report.Items[0].Month)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Report.Items[0].Count)
            </th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Report.Items)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.User)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Year)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Month)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Count)
                </td>
            </tr>
        }
    </tbody>
</table>
